pipeline {
    agent any
    stages {
        stage ('Installing_Docker!!') {
            steps {
                sh 'sudo apt-get update && apt-get upgrade -y'
                sh 'sudo apt-get install docker.io -y'
            }
        }
    }
}
